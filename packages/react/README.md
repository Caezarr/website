# @wonka/react

Private, framework-neutral React primitives for Wonka products and brand surfaces.

The package owns portable structure and styling contracts. Next.js adapters, fonts, and licensed brand assets stay in the application. Consumers must load `@wonka/tokens/tailwind.css` and include this package in Tailwind source detection.
